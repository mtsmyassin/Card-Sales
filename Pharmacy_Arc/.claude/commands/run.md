Start the Pharmacy Director Flask application.

Steps:
1. Check if port 5000 is already in use
2. If not running, start with: `cd C:\Users\mtsmy\OneDrive\Desktop\PharmacyApp && python app.py`
3. Wait for it to be ready, then verify: `curl http://127.0.0.1:5000/api/list`
4. Report status

If it fails:
- Check Python is installed and in PATH
- Check Flask is installed: `pip install flask`
- Check if port 5000 is blocked

App path: C:\Users\mtsmy\OneDrive\Desktop\PharmacyApp\app.py
